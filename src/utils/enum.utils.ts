export enum UserStatusEnumType {
    ACTIVE = 'Active',
    INACTIVE = 'InActive'
}
export enum UserEnumType {
    ADMIN = 'admin',
    CUSTOMER = 'customer'
}

export enum StatusEnumType {
    ACTIVE = 'Active',
    INACTIVE = 'InActive'
}

export enum DiscountEnumType {
    AMOUNT = 'Amount',
    PERCENTAGE = 'Percentage',
    NONE = 'None'
}

export enum HomeIntroStatusEnumType {
    TEXT_WITH_IMG = 'text-with-img',
    ONLY_IMAGE = 'only_img'
}

export enum HomeInstagramMediaEnumType {
    IMAGE = 'IMAGE',
    VIDEO = 'VIDEO'
}

export enum ProductReviewStatusEnumType {
    PENDING = 'Pending',
    CONFIRM = 'Confirm',
    REJECT = 'Reject',
}

export enum CouponCodeEnumType {
    FIXED = 'fixed',
    DISCOUNT = 'discount',
    BUY_ONE_GET_ONE = 'buyonegetone',
    FREE_SHIPPING = 'free_shipping',
    QUANTITY_DISCOUNT = 'quantity_discount',
    QUANTITY_DISCOUNT_CATEGORY_WISE = 'quantity_discount_category_wise',
}

export enum CheckoutEnumType {
    NORMAL = 'normal',
    GUEST = 'guest'
}

export enum ProductReturnStatusEnumType {
    APPROVED = 'Approved',
    UNAPPROVED = 'Unapproved',
    REJECTED = 'Rejected',
}